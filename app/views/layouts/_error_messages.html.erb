<% if object.errors.any? %>
	<div class='alert alert-error' >
		<h2><%= pluralize(object.errors.count, "error") %>:</h2>
		<ul>
		<% object.errors.each do |att, error| %>
			<li><%= error %></li>
		<% end %>
		</ul>
	</div>
<% end %>